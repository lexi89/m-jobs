<%= form_for(current_user.interests.find_by(followed_id: @company.id), html: {method: :delete}, remote: true) do |f| %>
	<div><%= hidden_field_tag :followed_id, @company.id %></div>
	<%= f.submit "Unfollow", class: "btn" %>
<% end %>